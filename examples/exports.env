# Using export prefix for POSIX shell compatibility

export DATABASE_URL=postgresql://localhost:5432/mydb
export API_KEY=sk-prod-xyz789
export DEBUG=false

export INDENTED_EXPORT=value
export TAB_EXPORT=value

# Regular definitions (no export)
LOCAL_VAR=local_value
INTERNAL_CONFIG=settings

# Keys starting with "export" (no space after)
exportKey=not_an_export
export_KEY=also_not_an_export
exportedValue=regular_key

# Export with different value types
export EMPTY=
export QUOTED="value with spaces"
export SINGLE='literal value'
