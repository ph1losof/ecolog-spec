# Line comments at the start

# Section: Database
DATABASE_HOST=localhost
DATABASE_PORT=5432

# Indented comment
# Tab-indented comment

### Multiple hash marks ###

# Inline comments (space before #)
API_KEY=sk-test-abc123 # Production API key
TIMEOUT=30             # In seconds
MAX_CONNECTIONS=100    # Tab before comment

# Hash NOT treated as comment (no preceding space)
HEX_COLOR=#FF5733
URL=https://example.com#fragment
ANCHOR=#top
START=#value

# Hash inside quotes (always literal)
CSS_COLOR="#333"
MESSAGE="This # is part of the value"
LITERAL='# Not a comment'
